---
generated_by: goal-prompt-generator
goal_prompt_generator_version: "1.0.1"
optimized_for: hermes-agent-goal
optimization_status: optimized
source_prompt_hash: "db904c9b7096ba7655399b737a05ca4ba5857402865def3d86b6145d0883d835"
generated_at: "2026-05-06T13:37:36Z"
domain: "software-development"
domain_confidence: "high"
---

# Hermes Mastra Plugin: Smarter, Faster, Unbreakable

## Goal

Upgrade the `hermes-mastra` Hermes Agent memory provider plugin so it is (a) measurably more performant on the hot path and on observation recall quality, (b) measurably smarter at choosing what to recall and what to persist, and (c) provably resilient — never breaking the host Hermes Agent process, never blocking a turn, never losing user-visible data, and never leaking across profiles, under any circumstance the plugin contract permits (server crash, disk full, malformed responses, queue saturation, network timeout, unauthenticated server, dependency drift, cron context, missing Bun, profile flip, partial init). Every change must follow this repo's `AGENTS.md` (BOOTSTRAP → RED → GREEN → REFACTOR; ≤200 LOC/file; ≤30 LOC/construct; depth ≤3; cognitive complexity ≤8; hot-path hooks return < 100 ms; `mise run quality` clean; commits squashed) and Hermes Agent's primitives (`PluginContext.register_*`, `register_hook`, `MemoryProvider` contract, non-blocking hooks, exclusive memory plugin, profile-keyed `resourceId`).

## Original Intent

The user said: "Update this plugin repo to make the mastra memory plugin even more performant and smart while following all of hermes agent's rules, philosophies, and primitives." A follow-up clarified: "update the plugin to ensure that it will never break under any circumstance." Both must be preserved. The plugin already has a solid baseline (sub-millisecond hot-path hooks, drop-oldest bounded queue, last-known-good recall cache, profile-resource isolation, plugin-failure-isolation tests). The job is to extend that baseline along three explicit axes — perf, smartness, unbreakability — without regressing it, without violating Hermes plugin isolation rules, and without bloating any file past the size policy.

## Domain

`software-development` (high confidence). Touches Python (provider, lifecycle, async runner, client, tests), TypeScript (Bun + Hono routes, Mastra Memory adapter), TOML (`mise.toml`, `pyproject.toml`), YAML (`plugin.yaml`, GitHub Actions, biome config), and shell (`scripts/*.sh`). Software-development constraints, ruthless cleanup, and engineering principles all apply.

## Assumptions

- The plugin remains an `kind: exclusive` memory provider; no scope expansion into context-engine or tool-only territory.
- Hermes Agent's `MemoryProvider` ABC, `PluginContext.register_memory_provider`, and `PluginContext.register_hook` semantics are stable on the version pinned by `scripts/check_compat_hermes.py`. Any drift surfaces as a `mise run compat:hermes` failure and the plan halts on it.
- `@mastra/memory` and `@mastra/libsql` track `latest`. Any breaking change in `Memory.recall`, `Memory.saveMessages`, `Memory.updateWorkingMemory`, `Memory.getWorkingMemory`, `Memory.listThreads`, `Memory.deleteThread`, or the `observationalMemory` config surface is a `mise run compat:mastra` failure and the plan halts on it.
- Bun ≥ 1.x and Python 3.11 are globally registered with mise (per `mise run setup`).
- The Hermes profile is the only tenancy boundary; no cross-profile features in this goal.
- "Never break under any circumstance" is interpreted within the plugin contract: the *plugin* must never raise into the host loop, never block a hook past the 100 ms budget, never corrupt or leak data across profiles, never wedge the agent if the Bun server is missing/dying/slow/wrong-versioned/auth-broken/disk-full/network-partitioned. It does **not** mean the plugin guarantees observation persistence when the underlying LibSQL DB is physically destroyed mid-write — that case must degrade to a logged, no-op fallback while the agent continues.
- Firecrawl (v1.16.0, FIRECRAWL_API_KEY-authenticated) and `opensrc` (v0.7.2) are available; their evidence is captured in `analysis/research/url-maps/` and the cached repos under `~/.opensrc/repos/`.
- Existing test suite (29 test modules) stays green; new tests are additive contract / chaos / perf tests.

## Non-Execution Guardrail

Do not execute this prompt while generating it. Only enhance, structure, validate, and save it.

## Isolated Generation Boundary

This prompt file was generated by explicit `goal-prompt-generator` invocation only. Do not invoke `goal-prompt-generator` automatically from Hermes Agent's `/goal` command, command routers, or persistent goal loop. Generation is complete after this Markdown file is saved and validated; any later execution must be an explicit, separate user action.

## Autonomous Execution Requirement

Ensure that you can operate everything autonomously without human intervention or a human in the loop, except where credentials, external approvals, legal authorization, payment authorization, or explicit safety constraints make human action mandatory. When human action is truly unavoidable, clearly document the blocker, the required human action, and the exact point at which autonomous execution can resume.

## Research and Source Validation Requirements

The downstream agent MUST validate every API/version/path claim against authoritative sources before writing implementation code. Required research and source-validation tooling:

### Firecrawl `map --limit 5000 --json --pretty`

Maps already produced for this goal (re-run if older than 7 days):

- Mastra docs: `analysis/research/url-maps/mastra.json` (204 links, root `https://mastra.ai/docs`)
- Hermes Agent docs: `analysis/research/url-maps/hermes.json` (317 links, root `https://hermes-agent.nousresearch.com/docs`)
- Bun docs: `analysis/research/url-maps/bun.json` (317 links, root `https://bun.com/docs`)
- Hono docs: `analysis/research/url-maps/hono.json` (84 links, root `https://hono.dev/docs`)
- libSQL/Turso docs: `analysis/research/url-maps/libsql.json` (253 links, root `https://docs.turso.tech`)
- mise docs: `analysis/research/url-maps/mise.json` (219 links, root `https://mise.jdx.dev`)

Use `firecrawl scrape`, `firecrawl extract`, `firecrawl search` against URLs surfaced by these maps for every authoritative claim the implementation makes. Cite each scraped page in the YAML's `validation_evidence.firecrawl.evidence[]`. Authoritative pages already pre-fetched in `analysis/research/scrapes/`:

- `mastra-memory-overview.json` (15 719 chars) — Mastra Memory configuration surface
- `hermes-memory-plugin.json` (17 722 chars) — Hermes `MemoryProvider` contract
- `hono-middleware.json` (6 755 chars) — Hono middleware composition rules
- `bun-http.json` (27 224 chars) — `Bun.serve` semantics, error handler, routing
- `libsql-quickstart.json` (10 593 chars) — libSQL/Turso TypeScript SDK quickstart

### `opensrc` source-validation

Open-source repos already fetched under `~/.opensrc/repos/`:

- `mastra-ai/mastra/main` — `packages/memory/src/`, `stores/libsql/src/storage/domains/memory/`, `packages/core/src/agent/save-queue/`, `packages/core/src/agent/agent.ts`. Verify every `Memory.*` API used by the plugin's `client.py` and `server/src/resources.ts`.
- `honojs/hono/main` — `src/middleware/timeout/index.ts`, `src/router/`. Verify Hono router/middleware contract used by `server/src/index.ts`.
- `tursodatabase/libsql-client-ts/main` — `packages/libsql-client/`. Verify libSQL connection / disconnect / disk-full behavior.
- `NousResearch/hermes-agent/main` — `agent/memory_provider.py` (280 LOC), `agent/memory_manager.py` (557 LOC), `hermes_cli/plugins.py:233` (`PluginContext`), `hermes_cli/plugins.py:528` (`register_hook`), `hermes_cli/plugins.py:1055` (`PluginContext.invoke_hook`), `hermes_cli/plugins.py:1163` (`invoke_hook` module-level). Verify every hook signature and every invariant (`invoke_hook` swallows callback exceptions; load order ≠ priority; kwargs are shared and must not be mutated).

### Existing knowledge-base files

Reuse and update when stale: `analysis/plugin-clash-analysis.md`, `analysis/plugin-contract.md`, `analysis/plugin-compatibility-matrix.yaml`, `analysis/source-analysis.md`, `analysis/memory-performance-plan.md`, `references/last-benchmark.md`, `references/provider-comparison.md`.

### Honesty rule

If Firecrawl or `opensrc` is unavailable at execution time, mark the affected validation step as an execution-time acceptance condition in the YAML and record the gap honestly in `validation_evidence` — never claim a tool was used when it was not.

## Scope

### In Scope

1. **Performance** — formal hot-path latency budget (every hook < 100 ms p99 with a slow client; sub-millisecond p50), measured by an extended `scripts/benchmark.py`; semantic recall warm-cache prefetch on profile/thread switch; reduced HTTP round-trips per turn (batched `save_turn`+`recall` where contract allows); request coalescing for back-to-back recalls; tightened `httpx.Timeout` profile per call site; LRU cap on `RecallCache` per (profile, thread) tuple; Bun `Bun.serve` `idleTimeout` + `error` handler tuned for fast hard-fail.
2. **Smartness** — observation deduplication on `write_observation` (idempotency key = sha256 of `(thread, profile, kind, normalized_text)`); profile-aware capacity hint thresholds tied to *both* MEMORY.md/USER.md fill *and* observation count; smarter `_capacity_hint` that suggests the precise tool to call (`mastra_observe` vs `mastra_search`) based on the missing context type; query-aware semantic-recall short-circuit when the user's last message contains an explicit recall verb (`remember`, `last time`, `we did`); Reflector-driven importance-weighted recall ranking; lineage-aware session continuity that reads parent thread observations into the new thread's prefetch on `do_session_switch` with `parent_session_id`.
3. **Unbreakability (resilience)** — circuit breaker around `MastraClient` (open after N consecutive failures, half-open after backoff, fail-closed to no-op); supervisor-style auto-restart of the Bun server on crash, gated by exponential backoff and a max-restarts/min cap; structured failure classification (`expected | retryable | defect`) preserved through `safe_call`; all `MastraClient` HTTP calls validate response shape before deserialization; runner queue saturation observability (counter + structured log) and explicit drop-oldest test under verified pathological load; disk-full / permission-denied / read-only-FS handling on PID file write, log file write, config write; chaos test suite (`tests/test_chaos_*.py`) that fault-injects on every hot-path entry point and asserts the agent still completes a turn; cron-context skip path proved via test; partial-init path (`do_initialize` raised before `_client` set) proved no-op via test; malformed JSON / non-JSON server responses tolerated; auth-token rotation tolerated mid-session; SIGTERM/SIGINT during `shutdown()` preserves in-flight writes when within `wait` budget.
4. **Tooling & docs** — extend `scripts/benchmark.py` with chaos and perf-regression modes; add `mise run chaos` task that runs the chaos suite under randomized fault injection; add `mise run bench:resilience` task that asserts hot-path budgets *with* fault injection enabled; refresh `analysis/memory-performance-plan.md`, `analysis/plugin-clash-analysis.md`, and `references/last-benchmark.md`; update `README.md`'s "What it does" + "Resilience guarantees" sections; update `CHANGELOG.md` with one squashed-commit-aligned entry.

### Out of Scope

- Cross-profile features, multi-tenant federation, or any change to the `kind: exclusive` policy.
- Replacing LibSQL with another store, adding a vector index backend, or wiring `@mastra/memory`'s vector recall to a non-default provider.
- New Hermes-side hooks not already in `plugin.yaml` (`on_session_end`, `on_session_switch`, `on_pre_compress`, `on_memory_write`, `on_delegation`).
- Any change to Hermes Agent core (`agent/memory_provider.py`, `agent/memory_manager.py`, `hermes_cli/plugins.py`) — read-only reference only.
- Anything that requires monkey-patching agent core modules or mutating kwargs passed to hooks.
- Documentation rewrite beyond what is needed to describe the new behaviour and the resilience guarantees.

## Execution Plan

### Phase 0: BOOTSTRAP

1. Run `mise run setup` and `mise run install` from a clean checkout; confirm `bun --version` and `python --version` resolve via mise; verify `.venv/bin/python -m pytest --collect-only -q` collects cleanly.
2. Run `mise run quality` and capture the green baseline; record `pytest --collect-only` totals as the floor.
3. Run `mise run bench` and persist `references/last-benchmark.json` as the perf floor.
4. Run `mise run compat:hermes` and `mise run compat:mastra`; halt the plan if either fails (do not proceed past BOOTSTRAP with drifted upstream contracts).
5. Re-validate every Firecrawl map's age; re-run `firecrawl map --limit 5000 --json --pretty` for any older than 7 days. Re-validate `opensrc path` for each open-source dep.
6. Create `tests/helpers/chaos.py`, `tests/helpers/fault_injectors.py`, and any new placeholder test files referenced by the RED tasks below; each must collect cleanly (BOOTSTRAP step of TDD).

### Phase 1: RED

Write tests that *fail for the right reason* — observable, contract-level, behaviour-driven. Never assert implementation details. RED tests must be committed before any GREEN production code is written; the YAML's R00 kickoff task enforces this hard gate.

1. **Hot-path budget under fault injection** — `tests/test_resilience_hot_path.py` asserts every hook returns in < 100 ms even when the client is slow (500 ms), the server is dead, the queue is saturated, the recall cache is being concurrently rotated, and the profile is mid-flip.
2. **Circuit breaker** — `tests/test_circuit_breaker.py` asserts that after N (configurable, default 5) consecutive `MastraClient` failures the breaker opens, all subsequent calls become deterministic no-ops within microseconds, the breaker auto-half-opens after `breaker_cooldown_seconds`, and one success closes it.
3. **Supervisor / server auto-recovery** — `tests/test_server_supervisor.py` asserts that a crashed Bun PID is detected within one health-check interval, that auto-restart runs with exponential backoff capped at `supervisor_max_restarts_per_minute`, and that the host process never blocks on a restart.
4. **Idempotent `write_observation`** — `tests/test_observation_dedup.py` asserts identical `(thread, profile, kind, normalized_text)` tuples produce one server-side observation; differs by any field produces two.
5. **Profile-flip safety** — `tests/test_profile_flip_safety.py` asserts that a profile flip mid-recall never returns observations from the previous profile, that `RecallCache` is cleared synchronously on flip detection, and that a synthetic lineage observation is written exactly once.
6. **Disk / permission failure** — `tests/test_filesystem_resilience.py` asserts PID write to a read-only path, log write to a missing dir, and config write to a permission-denied path all degrade to logged no-ops without raising.
7. **Malformed server response** — `tests/test_response_validation.py` asserts that non-JSON, partial-JSON, wrong-schema, and oversized (≥ 1 MB) `recall`/`semantic_search`/`search` responses are validated, rejected, and surfaced as a structured `MastraResponseError` consumed silently by `safe_call`.
8. **Queue saturation observability** — `tests/test_runner_saturation.py` asserts the bounded queue's drop-oldest counter increments under verified pathological load, that producer enqueue latency stays sub-microsecond, and that a structured log entry fires once per drop burst.
9. **Cron context** — `tests/test_cron_context_safety.py` asserts that `do_initialize` with `agent_context="cron"` or `platform="cron"` skips bring-up, that every subsequent hook is a no-op, that no HTTP call is made, and that `get_tool_schemas()` returns `[]`.
10. **Partial-init resilience** — `tests/test_partial_init.py` asserts that a `do_initialize` raise *after* `_profile` is set but *before* `_client` is assigned leaves the provider in a deterministic no-op state and never raises into Hermes.
11. **Auth rotation** — `tests/test_auth_rotation.py` asserts that an `auth_token_env` value change between two requests does not wedge the in-process `httpx.Client`; the plugin builds a new client lazily on first 401 and closes the old one.
12. **Smarter capacity hint** — `tests/test_capacity_hint_smartness.py` asserts that the `_capacity_hint` recommends `mastra_observe` only when MEMORY.md/USER.md is ≥ 50 % AND the observation count is below an actionable floor; otherwise recommends `mastra_search`.
13. **Lineage prefetch** — `tests/test_lineage_prefetch.py` asserts that `do_session_switch(parent_session_id=...)` warms the new thread's recall cache with the parent's last K observations on the next `prefetch()`.
14. **Compat & policy floor** — extend `tests/test_code_size_policy.py` to cover any new files under 200 LOC / 30 LOC / depth ≤ 3 / cognitive ≤ 8.

### Phase 2: GREEN

Write the **minimum** production code to make every RED test pass. No gold-plating, no incidental refactors.

1. Add `circuit_breaker.py` (≤ 100 LOC) — pure module: state machine `CLOSED | OPEN | HALF_OPEN`, configurable threshold + cooldown; thread-safe.
2. Wrap `MastraClient` HTTP methods through the breaker with a small adapter; preserve return semantics.
3. Add `response_guard.py` (≤ 80 LOC) — Pydantic-free, stdlib-only response validators (recall shape, observations shape, working_memory shape, search shape).
4. Add `supervisor.py` (≤ 120 LOC) — periodic health probe (off the hot path, on the runner), exponential backoff, max-restarts/min cap, fail-closed.
5. Add `observation_dedup.py` (≤ 60 LOC) — idempotency-key cache (LRU, bounded, per-profile), consulted by `do_memory_write` + `client.write_observation`.
6. Extend `recall_cache.py` to be per-(profile, thread) with bounded LRU; new public surface is additive.
7. Extend `lifecycle_helpers.py` and `provider_lifecycle.py` for lineage prefetch (`do_session_switch` enqueues a parent-recall-into-cache step).
8. Update `_capacity_hint` in `__init__.py` to use the smarter selection rule.
9. Server-side: add `Bun.serve({ idleTimeout, error })` config in `server/src/index.ts`; add a `/api/memory/healthz` route returning richer probe data; ensure every route returns within 5 s with a 503 + structured error rather than hanging.
10. Add typed config knobs to `server_config.py` and `mastra_options.py` (`breaker_threshold`, `breaker_cooldown_seconds`, `supervisor_max_restarts_per_minute`, `recall_cache_lru_size`, `dedup_lru_size`, `response_max_bytes`); validate at load time, redact in logs, fail-fast on malformed values.
11. Add `mise run chaos` and `mise run bench:resilience` tasks; both must be additive — `quality` stays green without them by default.
12. Update `analysis/memory-performance-plan.md`, `references/last-benchmark.md`, `README.md`, and `CHANGELOG.md` with the new guarantees and measured numbers.

### Phase 3: REFACTOR

1. Run `mise run quality`; ensure it stays green with the new tests.
2. Run `mise run bench` and `mise run bench:resilience`; assert no hot-path p99 regression vs the BOOTSTRAP baseline; assert the `under fault injection` p99 stays < 100 ms; persist new numbers to `references/last-benchmark.md`.
3. Run `mise run bench:compare` and confirm Mastra still wins or ties on the relevant axes.
4. Run `mise run compat:hermes` + `mise run compat:mastra`; both must stay green.
5. Run the chaos suite ten times in a row (`pytest -q tests/test_chaos_*.py --count 10` via `pytest-repeat` if needed, otherwise via a `for` loop) and assert every run is green.
6. Verify nothing in the final diff violates the file-size policy (no file > 200 LOC, no construct > 30 LOC, depth ≤ 3, cognitive complexity ≤ 8); split modules if they crept past.
7. Run the ruthless cleanup pass described under Software Development Constraints.
8. Squash to a single commit with the user's `[name] <email>` trailer; push to a topic branch; open the PR with the resilience guarantees and benchmark deltas in the body.

## Software Development Constraints

Ensure you always follow these rules:
- 200 LOC maximum per file.
- 30 LOC maximum per coding-language construct type, including functions, classes, interfaces, protocols, methods, components, hooks, services, schemas, tests, and comparable constructs.
- Maximum nesting depth of 3.
- For tests, nesting depth is measured relative to the test declaration.
- Local CI/CD must be configured and runnable before tests are written.
- Tests must be written before implementation code.
- Tests must focus on user-facing behavior, contracts, APIs, interfaces, workflows, and observable outcomes.
- Never test implementation details.
- Never write tests merely for the sake of increasing coverage.
- No TODOs, mocks, stubs, placeholders, fake behavior, fake services, simulated functionality, or incomplete implementations.
- Use only real tests, real behavior, real services, real functionality, real data flows, and real verification paths.
- Always operate with a BOOTSTRAP / RED / GREEN / REFACTOR TDD-first mindset.

At the end of the goal execution, perform a ruthless cleanup pass over the final diff. Remove every code change, file change, configuration change, dependency change, test change, documentation change, or generated artifact that is unrelated, incidental, speculative, exploratory, redundant, or extraneous to the successful completion of the approved goal. The final submitted change set must contain only the minimum necessary changes required to satisfy the goal and its validation criteria. If the goal is executed within a brownfield codebase, preserve the repository’s existing intent, architecture, conventions, abstractions, naming patterns, style, behavior, public contracts, tests, workflows, and integration assumptions unless the approved goal explicitly requires changing them. Before marking the goal complete, verify that the final change set does not introduce regressions, does not conflict with the surrounding repository context, does not degrade existing behavior, and does not leave behind temporary implementation scaffolding, abandoned experiments, dead code, unused dependencies, unused exports, debug logs, placeholder logic, TODOs, mocks, stubs, or broad refactors that are not required by the goal. If a change was made during execution but is not necessary for the final validated solution, revert it before completion.

## Software Engineering Core Principles

### Core Principles

1. **Treat programs as descriptions before execution** — Separate what work should happen from when and how it is executed. Design workflows, jobs, request handlers, and automation steps as explicit execution plans run through one well-defined execution boundary that owns cancellation, retries, logging, cleanup, dependency wiring, and failure reporting.
2. **Make expected failure part of the domain model** — Distinguish expected errors from unexpected defects. Model recoverable failures explicitly: validation failure, auth failure, payment declined, rate limited, and missing resource.
3. **Separate recoverable failures from defects** — Defects should terminate the operation and flow into diagnostics, not business recovery logic. Classify failures as user-correctable, retryable, compensatable, or defect; defects should fail fast with rich diagnostic context.
4. **Preserve failure causes, not just messages** — Errors should carry structured cause data: category, operation, retryability, input metadata, dependency name, correlation ID, and causal chain. Never reduce failures to `Error("something went wrong")`.
5. **Design every resource with a lifecycle** — Every resource needs an owner, scope, acquisition path, cleanup path, and failure behavior. Apply this to DB pools, browser sessions, temp files, locks, subprocesses, feature flags, queues, websocket connections, and test fixtures.
6. **Make cancellation and interruption first-class** — Long-running work must be cancellable. Request handlers, background jobs, CLI commands, AI agent loops, uploads, streams, and test runs should respond to cancellation and release resources deterministically.
7. **Use structured concurrency instead of ad hoc async work** — Parent operations should own child operations. When the parent is canceled, times out, or fails, children should be canceled or joined according to an explicit policy. Avoid untracked background tasks.
8. **Push dependencies to the boundary** — Business logic should depend on interfaces and capabilities, not concrete clients. Wire real implementations at the app boundary and test implementations at the test boundary.
9. **Keep service interfaces clean; move construction complexity elsewhere** — Service APIs should express business capabilities, not bootstrapping details. Assemble configuration, credentials, telemetry clients, connection pools, and dependency graphs outside the core interface.
10. **Validate and transform data at boundaries** — Parse external data once at the boundary, then operate internally on validated domain data. Apply this to HTTP requests, environment variables, webhooks, database rows, queue messages, LLM outputs, and config files.
11. **Treat configuration as typed, validated, and redacted** — Configuration should be parsed, validated, documented, and redacted before use. Missing or malformed config should fail startup, not appear as runtime mystery behavior.
12. **Build timeout and retry policy into external calls** — Every external call should have an explicit timeout, retry policy, backoff policy, idempotency strategy, and failure classification. No network call should wait indefinitely.
13. **Optimize based on measurement, not intuition** — Instrument before optimizing. Use latency histograms, error rates, queue depth, memory, CPU, flamegraphs, and business-level success metrics to find real bottlenecks.
14. **Make observability part of execution, not an afterthought** — Every meaningful operation should emit structured telemetry: request ID, user or tenant when safe, operation name, duration, outcome, failure cause, dependency calls, retry count, and cancellation reason.
15. **Cache deliberately, with invalidation semantics** — Cache expensive or remote computations only when you can define the key, freshness rule, invalidation rule, error behavior, and concurrency behavior.
16. **Serialize shared-state updates when consistency matters** — Shared mutable state should have a coordination mechanism: lock, transaction, queue, actor, compare-and-swap, database constraint, or single-writer policy.
17. **Compose small units into larger workflows** — Implement small, testable, composable units with explicit inputs, outputs, failures, dependencies, and lifecycle. Compose them into workflows rather than creating large imperative procedures.
18. **Keep provider-specific details behind stable application contracts** — Isolate vendor APIs behind internal contracts. Apply this to LLMs, payments, email, auth, storage, observability, search, browser automation, and deployment providers.

### Condensed Engineering Rubric

Use this as a checklist:

1. Can every operation state its success type, expected failures, dependencies, and side effects?
2. Are recoverable errors modeled separately from defects?
3. Are failure causes preserved with enough structure for diagnosis?
4. Does every resource have deterministic cleanup?
5. Are all long-running operations cancellable?
6. Are concurrent tasks owned by a parent scope or durable supervisor?
7. Are dependencies injected at the boundary rather than imported everywhere?
8. Is all external data validated and transformed before internal use?
9. Is configuration validated, redacted, and failed fast?
10. Does every external call have timeout, retry, and idempotency policy?
11. Is telemetry emitted as structured logs, metrics, and traces?
12. Are caches explicit about keys, TTL, invalidation, and consistency?
13. Are shared-state updates coordinated?
14. Are provider integrations hidden behind stable contracts?
15. Are optimizations driven by measurements?

### Bottom Line

Engineer software as explicit, typed, observable, cancellable, resource-safe workflows with clear failure semantics and boundary-driven dependency management.

## Acceptance Criteria

1. `mise run quality` passes from a clean checkout (format → lint → typecheck → test → security:audit → validate).
2. The full pytest suite (existing 29 modules + every new test module added under RED) passes; no regressions; no flakes when run 10× in a row.
3. `mise run bench` p50/p95/p99 for every hot-path hook (`prefetch`, `sync_turn`, `on_pre_compress`, `on_session_end`, `on_memory_write`, `on_delegation`) is **no worse than** the BOOTSTRAP baseline, with the new chaos-mode p99 still < 100 ms.
4. `mise run bench:resilience` reports zero failures across every fault-injection scenario; report is persisted to `references/last-benchmark.md`.
5. `mise run chaos` reports green ten consecutive runs.
6. `mise run compat:hermes` and `mise run compat:mastra` both green.
7. Every new file ≤ 200 LOC; every new function/class/method ≤ 30 LOC; nesting depth ≤ 3; cognitive complexity ≤ 8 (enforced by `tests/test_code_size_policy.py`).
8. The plugin never raises into the host loop in any test; `tests/test_chaos_*.py` proves this across at least 100 randomized fault sequences.
9. `RecallCache` never returns observations belonging to another profile, even under simulated profile flips at every hook entry point (`tests/test_profile_flip_safety.py`).
10. The Bun server is auto-recovered from a crash within `supervisor_max_restarts_per_minute` budget without blocking the host (`tests/test_server_supervisor.py`).
11. Disk-full / permission-denied / read-only-FS paths degrade to logged no-ops (`tests/test_filesystem_resilience.py`).
12. Malformed server responses do not raise into the host loop (`tests/test_response_validation.py`).
13. The cron-context skip path is enforced and tested (`tests/test_cron_context_safety.py`).
14. The smarter `_capacity_hint` is verified by `tests/test_capacity_hint_smartness.py`.
15. `analysis/memory-performance-plan.md`, `README.md`, and `CHANGELOG.md` describe the new guarantees and benchmark deltas.
16. Final commit is squashed and authored as `[<your-name>] <your-email>` with no co-author trailers (per `AGENTS.md`).
17. Every public-API claim made in code comments or docs is cited against an `opensrc` path or a Firecrawl-scraped URL captured under `analysis/research/`.

## Validation Commands

```bash
mise run setup
mise run install
mise run compat:hermes
mise run compat:mastra
mise run quality
mise run bench
mise run bench:resilience
mise run bench:compare
mise run chaos
.venv/bin/python -m pytest -q -k "chaos or resilience or breaker or supervisor or response_validation or filesystem or profile_flip or cron_context or partial_init or auth_rotation or capacity_hint or lineage_prefetch or observation_dedup or runner_saturation"
.venv/bin/python -m pytest -q tests/test_code_size_policy.py
.venv/bin/python -m pytest -q --count=10 tests/test_chaos_*.py    # if pytest-repeat is added; else use a shell loop
```

## Completion Definition

The goal is complete when, and only when:

1. All Acceptance Criteria above are met.
2. The PR is opened, CI is green, and the resilience guarantees + benchmark deltas are summarized in the PR body.
3. The plugin verifiably **never** raises into the host loop, **never** blocks a hook past 100 ms, **never** mixes profile data, and **never** wedges the agent across every fault scenario in the chaos suite.
4. The ruthless cleanup pass has been performed; the final diff contains only the minimum necessary changes.

## Failure Conditions

The goal is **incomplete or invalid** if any of the following hold:

1. `mise run quality`, `mise run bench`, `mise run bench:resilience`, `mise run chaos`, `mise run compat:hermes`, or `mise run compat:mastra` fails.
2. Any existing test regresses.
3. Any new file or construct violates the size policy.
4. The plugin raises into the host loop in any test.
5. Any hot-path hook exceeds the 100 ms p99 budget under fault injection.
6. Cross-profile data leaks in any chaos scenario.
7. The final diff contains scaffolding, dead code, mocks, stubs, placeholders, debug logs, or unrelated drive-by changes.
8. Any API/version claim in code or docs is not backed by an `opensrc` path or a Firecrawl-cited URL.
9. The handoff `/goal` line printed by the generator is altered, persisted into either generated artifact, or executed by the generator itself.

## Final Output Requirements

1. A green CI run on the merged PR; a single squashed commit with the user's `[name] <email>` trailer.
2. Updated `analysis/memory-performance-plan.md` describing the new resilience layer, the perf budgets, the smartness rules, and how each is enforced.
3. Updated `references/last-benchmark.md` (and `references/last-benchmark.json`) with chaos-mode + resilience-mode numbers.
4. Updated `README.md` "Resilience guarantees" section enumerating every promise the plugin now makes.
5. Updated `CHANGELOG.md` entry under the new version.
6. New tests under `tests/test_chaos_*.py` and the RED set listed above; new helpers under `tests/helpers/`.
7. New production modules: `circuit_breaker.py`, `response_guard.py`, `supervisor.py`, `observation_dedup.py`; extended `recall_cache.py`, `provider_lifecycle.py`, `lifecycle_helpers.py`, `__init__.py`, `server_config.py`, `mastra_options.py`, `server/src/index.ts`.
8. New mise tasks: `chaos`, `bench:resilience`.
9. The companion `<project>-<intent>-tdd-tasks.yaml` file produced by the goal-prompt-generator (kept in sync with this Markdown) — the only mutable runtime state during execution.
